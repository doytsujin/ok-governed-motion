[package]
name = "govmotion-wasm"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "The wasm-bindgen seam the browser calls"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
gov = { path = "../gov" }
robot = { path = "../robot" }
wasm-bindgen = "0.2"
